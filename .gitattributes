# Normalize every text file to LF in the repository and in all working
# trees (Git for Windows included); binary files are auto-detected.
* text=auto eol=lf

# Binary assets stay untouched.
*.png binary
